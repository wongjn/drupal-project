{#
/**
 * @file
 * Default theme implementation for an SVG icon.
 *
 * SVG icons are pulled from one master sheet. This collapses potentially many
 * requests into 1, optimized for HTTP 1.1.
 *
 * Available variables:
 * - id: The ID of the <symbol> element to use from the spritesheet.
 * - attributes: Attributes string to set on the <svg> element.
 * - sheet_url: The path to the SVG sprite sheet.
 *
 * @see template_preprocess_icon();
 *
 * @ingroup themable
 */
#}
{% spaceless %}
<svg{{ attributes }}>
  <use xlink:href="{{ sheet_url }}#{{ id }}"/>
</svg>
{% endspaceless %}
