services:
  {{ NAME }}_icons.manager:
    class: Drupal\{{ NAME }}_icons\Manager
    arguments: ['@cache.discovery', '@cache_tags.invalidator', '@theme_handler']

  {{ NAME }}_icons.twig_extension:
    class: Drupal\{{ NAME }}_icons\Template\TwigExtension
    tags:
      - { name: twig.extension }
